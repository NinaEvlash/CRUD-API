{
  "name": "crud-api",
  "version": "1.0.0",
  "scripts": {
    "start:dev": "ts-node-dev --loader ts-node/esm --respawn --transpile-only src/cluster.ts",
    "build": "tsc",
    "start:prod": "npm run build && node dist/cluster.js",
    "start:test": "ts-node src/tests/apiTest.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/uuid": "^10.0.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
